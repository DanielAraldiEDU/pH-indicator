import { memo } from 'react';
import { View } from 'react-native';

import { styles } from './styles';

function PhButtonLine() {
  return <View style={styles.container} />;
}

export default memo(PhButtonLine);
