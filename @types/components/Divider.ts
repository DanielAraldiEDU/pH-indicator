export type DividerVariant = 'default' | 'header' | 'footer';

export interface DividerProps {
  /**
   * @description Provides the variant of the divider.
   *
   * @default 'default'
   */
  variant?: DividerVariant;
}